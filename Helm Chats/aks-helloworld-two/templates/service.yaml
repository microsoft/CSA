apiVersion: v1
kind: Service
metadata:
  name: {{ include "aks-helloworld-two.fullname" . }}
  labels:
    {{- include "aks-helloworld-two.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aks-helloworld-two.selectorLabels" . | nindent 4 }}
